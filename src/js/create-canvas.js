export function createCanvas(width, height) {
    let canvas = window.document.createElement('canvas');
    canvas.width = width;
    canvas.height = height;
    return canvas;
  }
  
  
  
  // WEBPACK FOOTER //
  // ./src/images_processor/create-canvas.js